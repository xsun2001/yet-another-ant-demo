export class Barrack {
    id: number;
    player: number;
    x: number;
    y: number;
    cd: number;
    constructor(id: number, player: number, x: number, y: number, cd: number) {
      this.id = id;
      this.player = player;
      this.x = x;
      this.y = y;
      this.cd = cd;
    }
  }